# py-magi

Python backend and launcher for MAGI. Packages live at this project root
(`bus`, `agent`, `startup`, …).

The repository-level overview is in [`../README.md`](../README.md); the operator
UI is the sibling [`../app/`](../app/) project.
