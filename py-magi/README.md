# py-magi

Python backend and launcher for MAGI. The import package remains `magi`.

The repository-level overview is in [`../README.md`](../README.md); the React
frontend is the sibling [`../WebUI/`](../WebUI/) project.
