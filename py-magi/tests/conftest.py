"""Shared pytest configuration.

Browser sessions are v5 signed MAGIS-admin / local-contact projection payloads.
Tests must mint them through the public login helpers; unsigned contact-id
cookies are intentionally not accepted in test mode.
"""
