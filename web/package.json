{
  "name": "magi-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MAGI web frontend — single Vite SPA holding the admin CRUD (C1) and chat-style console (C7) as route groups.",
  "scripts": {
    "dev": "echo 'dev script lands in C1.0b' && exit 1",
    "build": "echo 'build script lands in C1.0b' && exit 1",
    "preview": "echo 'preview script lands in C1.0b' && exit 1",
    "lint": "echo 'lint script lands in C1.0b' && exit 1",
    "test": "echo 'test script lands in C1.0b' && exit 1",
    "generate-types": "echo 'generate-types lands in C1.2' && exit 1"
  },
  "engines": {
    "node": ">=20"
  }
}